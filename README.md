# Рядом56

Локальный MVP сервиса для Сакмарского района Оренбургской области и Оренбурга:
объявления/рынок, справочник, веб-админка, Android (Flutter).

## Состав

| Папка | Что внутри |
|-------|------------|
| `backend/` | FastAPI + SQLite |
| `admin/` | React-админка (Vite), на проде `https://legac.ru/console/` |
| `site/` | Публичная визитка `https://legac.ru/` |
| `mobile/` | Flutter-приложение (Android) |

## Быстрый старт (локально)

### 1. API

```bat
cd backend
python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
```

- Документация API: http://127.0.0.1:8000/docs
- Админ **локально**: `admin@ryadom56.ru` / `admin123`. На проде пароль другой, дефолт не использовать.

### 2. Админка

```bat
cd admin
npm install
npm run dev
```

Откроется http://127.0.0.1:5173

На проде админка спрятана: **https://legac.ru/console/** (сборка с относительным `base: './'`).

### 3. Android (Flutter)

Нужен установленный [Flutter SDK](https://docs.flutter.dev/get-started/install/windows).

```bat
cd mobile
flutter create . --project-name ryadom56 --org ru.ryadom56
flutter pub get
flutter run
```

> `flutter create .` один раз дописывает android/ios-обвязку к уже готовому `lib/`.

**API URL на телефоне:** `https://legac.ru/api` (собирается через GitHub Actions).  
**Локально / эмулятор:** `http://10.0.2.2:8000/api`.

## Что уже есть

### MVP-1
- Регистрация: имя, email, пароль, телефон (опционально), населённый пункт, галочки соглашений
- 47 населённых пунктов Сакмарского района + Оренбург (две «Марьевки» разделены по сельсоветам)
- Объявления с модерацией (товары, услуги, работа, аренда, отдам, потеряшки)
- Справочник (школы, больницы, магазины и т.д.)
- Админка: сводка, модерация, справочник, роли пользователей (admin / moderator / editor)
- Юридические тексты: соглашение, политика, правила объявлений (`/api/legal`)

### MVP-2 / MVP-2+
- Афиша: события, обложки, отложенная публикация, календарь/шаринг/напоминание
- Транспорт: вкладка, остановки, будни/выходные, избранное
- Новости района и срочный баннер на ленте
- Быстрые фильтры «Работа» / «Потеряшки»
- Обновления APK с сервера

## Дальше

Туризм-гид, обращения в администрацию, Яндекс.Карты SDK в UI, push (FCM), хостинг.

## Важно

- Прод: домен **legac.ru**, визитка `https://legac.ru/`, админка `https://legac.ru/console/`, API `https://legac.ru/api`.
- Пароль админа смените перед широкой раздачей.
- `secret_key` в `backend/app/core/config.py` — только для разработки.
