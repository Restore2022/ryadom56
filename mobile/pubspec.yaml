name: ryadom56
description: Ryadom56 — Sakmarsky district app
publish_to: "none"
version: 0.23.1+36

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.5.0
  http_parser: ^4.1.2
  shared_preferences: ^2.5.3
  provider: ^6.1.5
  google_fonts: ^8.0.0
  url_launcher: ^6.3.2
  device_info_plus: ^13.0.0
  package_info_plus: ^10.0.0
  image_picker: ^1.1.2
  share_plus: ^13.3.0
  path_provider: ^2.1.5
  open_filex: ^4.7.0
  crypto: ^3.0.7
  local_auth: ^2.3.0
  local_auth_android: ^1.0.56
  firebase_core: ^4.0.0
  firebase_messaging: ^16.0.0
  flutter_local_notifications: ^19.0.0
  geolocator: ^13.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
