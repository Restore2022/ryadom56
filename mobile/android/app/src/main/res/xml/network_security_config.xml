<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- LAN API during MVP; switch to HTTPS-only when domain is ready -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
